<li>
    <p>
        <%= comment.content %>
        <% if (locals.user && locals.user.id == comment.user.id){ %>
            <small>
                <a href="/comments/destroy/<%= comment.id %>"><i class="fa-solid fa-trash-can"></i></a>
            </small>
        <% } %>
        <br>
        <small> 
            <%= comment.user.name %>
        </small>
    </p>
</li>